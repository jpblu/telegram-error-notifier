{
  "name": "jpblu/telegram-error-notifier",
  "description": "A PHP library to send messages to a Telegram bot in case of error. Compatible with Laravel.",
  "type": "library",
  "autoload": {
    "psr-4": {
      "TelegramNotifier\\": "src/"
    }
  },
  "require": {
    "php": ">=7.4",
    "guzzlehttp/guzzle": "^7.0"
  },
  "extra": {
    "laravel": {
      "providers": [
        "TelegramNotifier\\Laravel\\TelegramNotifierServiceProvider"
      ]
    }
  }
}
